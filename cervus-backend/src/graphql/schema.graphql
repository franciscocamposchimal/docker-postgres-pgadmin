
type Query {
    user(id: ID!): User!
    users: [User!]!
    post(id: ID!): Post!
    posts: [Post!]!
}

type Mutation {
    createUser(data: AuthPayload): User!
    signup(data: SignUpInput): AuthPayload!
    login(data: LoginInput): AuthPayload!
    createPost(data: PostInput): Post!
}