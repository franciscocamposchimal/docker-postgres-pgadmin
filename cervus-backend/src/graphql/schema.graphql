type Message {
    id: ID!
    description: String!
}

input MessageInput {
    description: String!
}

type Query {
    messages: [Message]!
}

type Mutation {
    createMessage(data: MessageInput): Message!
}